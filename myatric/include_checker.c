#include <stdio.h>
#include <stdlib.h>
//~ #include <string.h>
#include "myatric.h"

char* find_way(char* name, int local_flag){
	char* ans = (char*)malloc(MAX_PATH);
	sprintf(ans, "%s%s%d", local_flag : "./" ? "/usr/includes/", name, 0);
	return 0;
}

void include_check(char* str){
	
}
